# Template only: do not place secrets in this file.
# Copy values to a private local environment when a hosting Agent requires them.

SKILL_CONFIG_PATH=config.yaml
SELF_BRAND_NAME=
SELF_BRAND_POSITIONING=
KNOWLEDGE_SYNC_ENABLED=false
KNOWLEDGE_REPOSITORY=
KNOWLEDGE_INTEGRATION_BRANCH=develop
KNOWLEDGE_REVIEW_BRANCH_PREFIX=note/
OFFICIAL_TOP_UP_PAGE_LIMIT=5

# Optional tool endpoints. Leave blank unless the local Agent is configured to use them.
EAGLE_API_URL=
EAGLE_API_TOKEN=
