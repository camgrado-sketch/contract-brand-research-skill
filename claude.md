# Compatibility note

Claude Code reads `CLAUDE.md` as the active project instruction file. This lowercase file exists only for repository conventions that expect `claude.md`; keep it aligned with `CLAUDE.md` and do not place additional instructions here.

The shared Skill and only packaged configuration field schema live under `skills/contract-brand-research/`. Follow `AGENTS.md` for branch workflow, validation commands, and safety rules.
