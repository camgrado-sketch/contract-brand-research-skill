# Compatibility note

Claude Code reads `CLAUDE.md` as the active project instruction file. This lowercase file exists only for repository conventions that expect `claude.md`; keep its content aligned with `CLAUDE.md` and do not place additional instructions here.
